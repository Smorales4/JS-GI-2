firstValue = prompt("Give me the first value.")
secondValue = prompt("Give me the second value.")

var min = function(firstValue, secondValue) {
    return Math.min(firstValue <= secondValue) ? firstValue : secondValue;
}

